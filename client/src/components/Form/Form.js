import React from "react";
import makeStyles from "./styles";

const Form = () => {
  const classes = makeStyles();
  return (
    <div class="form">
      <h1>FORM</h1>
    </div>
  );
};

export default Form;
